{
  "_comment": "Nosana job definition for an Ollama server exposing the OpenAI-compatible /v1/chat/completions and /v1/embeddings endpoints that OpenAiCompatibleLlmClient calls. Post with: nosana job post ./nosana/job-definition.json --market <MARKET_ADDRESS> --timeout <MINUTES>. Fill in every <PLACEHOLDER> before posting -- see nosana/README.md.",
  "version": "0.1",
  "type": "container",
  "ops": [
    {
      "id": "ollama-server",
      "type": "container/run",
      "args": {
        "image": "<YOUR_DOCKERHUB_USERNAME>/matchup-ollama:latest",
        "gpu": true,
        "expose": [
          { "port": 11434 }
        ],
        "env": {
          "CHAT_MODEL": "llama3.1",
          "EMBEDDING_MODEL": "nomic-embed-text"
        }
      }
    }
  ],
  "meta": {
    "trigger": "dashboard",
    "system_requirements": {
      "required_vram": 12
    }
  }
}
